<h4>Categorias: </h4>

{{#each categorias}}
    <div class="card">
        <div class="card-body">
            <a href="/categorias/{{slug}}"><h4>{{nome}}</h4></a>
        </div>
    </div>
{{else}}

{{/each}}